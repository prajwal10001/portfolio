# Oracle Cloud Deployment Flowchart

## 🚀 Complete Deployment Process

```
┌─────────────────────────────────────────────────────────────────┐
│                    START: Oracle Cloud Deployment                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │ Have OCI       │
                    │ Account?       │
                    └───┬────────┬───┘
                        │ YES    │ NO
                        │        │
                        │        ▼
                        │   ┌────────────────────────┐
                        │   │ Create OCI Account     │
                        │   │ oracle.com/cloud/free  │
                        │   │ (5 minutes)            │
                        │   └────────┬───────────────┘
                        │            │
                        └────────────┼───────────────┐
                                     │               │
                                     ▼               │
                        ┌─────────────────────────┐  │
                        │ Choose Deployment       │  │
                        │ Option:                 │  │
                        │                         │  │
                        │ 1. Compute VM ⭐        │  │
                        │ 2. Docker Compose       │  │
                        │ 3. Container Instances  │  │
                        └────────┬────────────────┘  │
                                 │                   │
                ┌────────────────┼────────────────┐  │
                │                │                │  │
                ▼                ▼                ▼  │
      ┌─────────────┐  ┌─────────────┐  ┌──────────────┐
      │   Option 1  │  │   Option 2  │  │   Option 3   │
      │ Compute VM  │  │   Docker    │  │  Container   │
      │  (FREE)     │  │  Compose    │  │  Instances   │
      └──────┬──────┘  └──────┬──────┘  └──────┬───────┘
             │                │                 │
             │                │                 │
             └────────────────┼─────────────────┘
                              │
                              ▼
        ┌───────────────────────────────────────────┐
        │ OPTION 1: Compute Instance (Recommended)  │
        └───────────────────┬───────────────────────┘
                            │
                            ▼
            ┌─────────────────────────────────┐
            │ 1. Create Compute Instance      │
            │    - Shape: VM.Standard.E4.Flex │
            │    - Image: Ubuntu 22.04        │
            │    - OCPU: 1, RAM: 4-6 GB       │
            │    - Enable Public IP           │
            │    (5 minutes)                  │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ 2. Configure Security List      │
            │    - Port 80 (HTTP)             │
            │    - Port 443 (HTTPS)           │
            │    - Port 8000 (Backend)        │
            │    (3 minutes)                  │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ 3. SSH into Instance            │
            │    ssh -i key.pem ubuntu@<IP>   │
            │    (1 minute)                   │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ 4. Clone Repository             │
            │    git clone <repo-url>         │
            │    (2 minutes)                  │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ 5. Run Deployment Script        │
            │    bash oci-deploy.sh           │
            │    - Installs dependencies      │
            │    - Configures services        │
            │    - Builds frontend            │
            │    - Starts backend with PM2    │
            │    - Configures Nginx           │
            │    (5-10 minutes)               │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ 6. Enter API Keys               │
            │    - Azure Speech Key           │
            │    - Azure OpenAI Key           │
            │    - Cartesia Key               │
            │    (2 minutes)                  │
            └─────────────┬───────────────────┘
                          │
                          ▼
            ┌─────────────────────────────────┐
            │ DEPLOYMENT COMPLETE! ✅          │
            └─────────────┬───────────────────┘
                          │
                          ▼
        ┌───────────────────────────────────────────┐
        │            TESTING & VERIFICATION          │
        └───────────────────┬───────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            │               │               │
            ▼               ▼               ▼
    ┌──────────────┐ ┌─────────────┐ ┌────────────┐
    │ Test Backend │ │Test Frontend│ │ Test Voice │
    │ Port 8000    │ │  Port 80    │ │   Agent    │
    └──────┬───────┘ └──────┬──────┘ └──────┬─────┘
           │                │               │
           ▼                ▼               ▼
    ┌──────────────┐ ┌─────────────┐ ┌────────────┐
    │ curl <IP>:   │ │ Open http://│ │ Click      │
    │ 8000/        │ │ <IP>/ in    │ │ "Connect"  │
    │              │ │ browser     │ │ & Speak    │
    │ Expected:    │ │             │ │            │
    │ {"status":   │ │ Should load │ │ Should get │
    │ "ok"}        │ │ portfolio   │ │ response   │
    └──────┬───────┘ └──────┬──────┘ └──────┬─────┘
           │                │               │
           └────────────────┼───────────────┘
                            │
                            ▼
                  ┌──────────────────┐
                  │ All Tests Pass?  │
                  └────┬─────────┬───┘
                       │ YES     │ NO
                       │         │
                       │         ▼
                       │    ┌──────────────────────┐
                       │    │ Troubleshooting:     │
                       │    │ - Check PM2 status   │
                       │    │ - Check nginx status │
                       │    │ - View logs          │
                       │    │ - Check Security List│
                       │    └──────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────┐
        │  🎉 DEPLOYMENT SUCCESSFUL! 🎉    │
        │                                  │
        │  Frontend: http://<IP>/          │
        │  Backend:  http://<IP>:8000/     │
        │                                  │
        │  Cost: $0/month (Always Free)    │
        └──────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────┐
        │       OPTIONAL NEXT STEPS         │
        └──────────────┬───────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
┌───────────────┐ ┌──────────┐ ┌─────────────┐
│ Setup Custom  │ │ Enable   │ │   Setup     │
│    Domain     │ │   SSL    │ │ Monitoring  │
│   (Optional)  │ │(Certbot) │ │ (PM2/OCI)   │
└───────────────┘ └──────────┘ └─────────────┘
        │              │              │
        └──────────────┼──────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  PRODUCTION     │
              │     READY!      │
              └─────────────────┘
```

---

## 📊 Timeline Breakdown

| Step | Duration | Cumulative |
|------|----------|------------|
| Create OCI Account | 5 min | 5 min |
| Create Compute Instance | 5 min | 10 min |
| Configure Security List | 3 min | 13 min |
| SSH into Instance | 1 min | 14 min |
| Clone Repository | 2 min | 16 min |
| Run Deployment Script | 10 min | 26 min |
| Enter API Keys | 2 min | 28 min |
| Test & Verify | 5 min | **33 min** |

**Total Time: ~30-35 minutes** from start to production! ⚡

---

## 🔄 Deployment Architecture Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         YOUR CODE                           │
│                      (GitHub Repository)                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ git clone
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              OCI Compute Instance (Ubuntu 22.04)            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                  oci-deploy.sh                        │  │
│  │  ┌───────────┐  ┌──────────┐  ┌──────────────────┐  │  │
│  │  │  Install  │→ │ Install  │→ │ Install Nginx &  │  │  │
│  │  │  Python   │  │  Node.js │  │      PM2         │  │  │
│  │  └───────────┘  └──────────┘  └──────────────────┘  │  │
│  │         │              │                 │           │  │
│  │         └──────────────┼─────────────────┘           │  │
│  │                        │                             │  │
│  │                        ▼                             │  │
│  │         ┌──────────────────────────────────┐         │  │
│  │         │  Build & Deploy Applications     │         │  │
│  │         └──────────────┬───────────────────┘         │  │
│  │                        │                             │  │
│  │         ┌──────────────┴───────────────┐             │  │
│  │         │                              │             │  │
│  └─────────┼──────────────────────────────┼─────────────┘  │
│            │                              │                │
│            ▼                              ▼                │
│  ┌──────────────────┐          ┌──────────────────────┐   │
│  │  Frontend (Port 80)│        │ Backend (Port 8000)  │   │
│  │                  │          │                      │   │
│  │  Nginx           │◄────────►│  Python FastAPI      │   │
│  │  ├─ React SPA    │  WebSocket│  ├─ Pipecat AI     │   │
│  │  ├─ Static Files │          │  ├─ Azure OpenAI    │   │
│  │  └─ Gzip Enabled │          │  ├─ Azure Speech    │   │
│  │                  │          │  └─ Cartesia TTS    │   │
│  │  Managed by:     │          │                      │   │
│  │  systemd         │          │  Managed by: PM2     │   │
│  └──────────────────┘          └──────────────────────┘   │
│                                                            │
│  Public IP: xx.xx.xx.xx                                    │
└────────────────────────┬───────────────────────────────────┘
                         │
                         │ Internet
                         │
                         ▼
                 ┌───────────────┐
                 │   End Users   │
                 │               │
                 │ - Web Browser │
                 │ - Microphone  │
                 │ - Speakers    │
                 └───────────────┘
```

---

## 📁 File Structure After Deployment

```
/home/ubuntu/<repo-name>/
├── .env                          # Environment variables (created by script)
├── .gitignore                    # Updated with SSH keys
├── package.json                  # Frontend dependencies
├── requirements.txt              # Backend dependencies
├── vite.config.ts               # Vite configuration
├── tsconfig.json                # TypeScript configuration
│
├── Dockerfile.backend           # Backend container (NEW)
├── Dockerfile.frontend          # Frontend container (NEW)
├── docker-compose.yml           # Multi-container setup (NEW)
├── nginx.conf                   # Nginx configuration (NEW)
├── oci-deploy.sh               # Deployment script (NEW)
│
├── ORACLE_CLOUD_DEPLOYMENT.md            # Full guide (NEW)
├── ORACLE_DEPLOYMENT_CHECKLIST.md        # Checklist (NEW)
├── ORACLE_QUICK_REFERENCE.md             # Quick ref (NEW)
├── ORACLE_DEPLOYMENT_SUMMARY.md          # Summary (NEW)
│
├── src/                         # Frontend source code
│   ├── component/
│   │   ├── VoiceAgent.tsx      # Voice agent component
│   │   └── ui/                 # UI components
│   ├── index.css
│   └── ...
│
├── custom_stt_server_websocket.py  # Backend WebSocket server
├── pipecat_server.py              # Pipecat configuration
│
├── dist/                        # Frontend build (after npm run build)
│   ├── index.html
│   ├── assets/
│   └── ...
│
└── venv/                        # Python virtual environment
    └── ...

Deployed Locations:
├── /var/www/html/              # Nginx serves frontend from here
│   └── (contents of dist/)
│
└── PM2 Process:
    └── voice-agent-backend     # Running backend on port 8000
```

---

## 🎯 Decision Tree: Which Deployment Option?

```
                    START
                      │
                      ▼
        ┌────────────────────────┐
        │ Do you want to manage  │
        │ infrastructure?        │
        └──────┬────────┬────────┘
               │ YES    │ NO
               │        │
               ▼        ▼
     ┌─────────────┐  ┌──────────────────┐
     │ Need Docker │  │ Use Container    │
     │ isolation?  │  │ Instances        │
     └──┬────┬─────┘  │ (Serverless)     │
        │ NO │ YES    │ $10-30/month     │
        │    │        └──────────────────┘
        ▼    ▼
  ┌────────┐ ┌──────────────┐
  │ Use    │ │ Use Docker   │
  │ Direct │ │ Compose      │
  │ VM     │ │              │
  │ ⭐FREE │ │ FREE         │
  └────────┘ └──────────────┘
     │            │
     └────────┬───┘
              │
              ▼
     ┌────────────────┐
     │ DEPLOY! 🚀     │
     └────────────────┘
```

**Recommendation for you:** Direct VM (Option 1) - Always Free, simple, reliable! ⭐

---

## 🔧 Component Interaction Flow

```
User Opens Website
       │
       ▼
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ HTTP GET /
       │
       ▼
┌──────────────────┐
│  Nginx (Port 80) │
│  Returns React   │
│     SPA          │
└──────┬───────────┘
       │ HTML/JS/CSS
       │
       ▼
┌──────────────────┐
│  React App Loads │
└──────┬───────────┘
       │
       │ User clicks "Connect"
       │
       ▼
┌──────────────────────────┐
│  VoiceAgent.tsx          │
│  Creates WebRTC/WebSocket│
│  Connection              │
└──────┬───────────────────┘
       │ WebSocket Connect
       │ ws://<IP>:8000/ws
       │
       ▼
┌────────────────────────────┐
│ Backend (Port 8000)        │
│ custom_stt_server_websocket│
└──────┬─────────────────────┘
       │
       │ WebSocket Connected
       │
       ▼
┌─────────────────────┐
│  User Speaks        │
│  (Microphone)       │
└──────┬──────────────┘
       │ Audio Stream
       │
       ▼
┌─────────────────────────┐
│  Azure Speech (STT)     │
│  Speech → Text          │
└──────┬──────────────────┘
       │ Text
       ▼
┌─────────────────────────┐
│  Azure OpenAI (LLM)     │
│  Generate Response      │
└──────┬──────────────────┘
       │ Response Text
       ▼
┌─────────────────────────┐
│  Cartesia (TTS)         │
│  Text → Speech          │
└──────┬──────────────────┘
       │ Audio Stream
       ▼
┌─────────────────────────┐
│  User Hears Response    │
│  (Speakers)             │
└─────────────────────────┘
```

---

## ✅ Verification Flowchart

```
       Deployment Complete
              │
              ▼
    ┌─────────────────┐
    │ Test Backend    │
    │ curl <IP>:8000/ │
    └────┬──────┬─────┘
         │ PASS │ FAIL
         │      │
         │      ▼
         │   ┌──────────────────┐
         │   │ Check PM2 status │
         │   │ Check logs       │
         │   │ Restart service  │
         │   └──────────────────┘
         │
         ▼
    ┌──────────────────┐
    │ Test Frontend    │
    │ Open http://<IP> │
    └────┬──────┬──────┘
         │ PASS │ FAIL
         │      │
         │      ▼
         │   ┌───────────────────┐
         │   │ Check nginx       │
         │   │ Check dist/ files │
         │   │ Rebuild frontend  │
         │   └───────────────────┘
         │
         ▼
    ┌────────────────────┐
    │ Test Voice Agent   │
    │ Connect & Speak    │
    └────┬──────┬────────┘
         │ PASS │ FAIL
         │      │
         │      ▼
         │   ┌──────────────────────┐
         │   │ Check WebSocket      │
         │   │ Check API keys       │
         │   │ Check browser console│
         │   └──────────────────────┘
         │
         ▼
    ┌────────────────┐
    │ ALL TESTS PASS │
    │      ✅        │
    └────────────────┘
         │
         ▼
    ┌────────────────┐
    │  PRODUCTION    │
    │     READY!     │
    └────────────────┘
```

---

**🎉 You now have a complete visual guide for Oracle Cloud deployment!**

This flowchart shows every step from account creation to production deployment.
